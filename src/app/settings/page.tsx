import { KeyRound, ShieldCheck } from "lucide-react";
import { WorkbenchFrame } from "@/components/universal/WorkbenchFrame";
export default function SettingsPage() { return <WorkbenchFrame current="settings"><div className="uc-list-page"><span className="uc-eyebrow">LOCAL SETTINGS</span><h1>设置</h1><p>模型配置只在本地工作区使用，不写入仓库。高级模式才会出现模型指令和生成参数。</p><div className="uc-settings-grid"><article className="uc-setting-card"><KeyRound size={20} /><h2>模型配置</h2><p>首页与工作区默认使用本地结构化演示编译。需要真实 AI 时，在 `.env.local` 配置现有文本/图片接口，或在工作区的高级模式填写图片模型。</p><code>RIGHT_CODES_API_KEY<br />RIGHT_CODES_IMAGE_API_KEY</code></article><article className="uc-setting-card"><ShieldCheck size={20} /><h2>安全边界</h2><p>网页成果只进入 sandbox iframe。预览不授予主页面 Cookie、localStorage、文件系统、网络请求或顶层跳转权限。</p></article></div></div></WorkbenchFrame>; }
